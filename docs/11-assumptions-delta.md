# 11 — Assumptions Delta: Personal → Public Multi-Tenant

> Status: **draft for discussion** · 2026-09-13 · Section-by-section audit of every assumption in [00](00-requirements.md)–[07](07-search-contract.md) invalidated or reshaped by the pivot: starwatch becomes a **public, no-login, multi-tenant service** — anyone enters any GitHub username, we index that user's **public** stars, expose public full-text + semantic search, use **one service token** for rate limits, sync **manually / eager-lazy**, and import public GitHub Lists as groups where visible.
> **Updated 2026-09-13 (free-tier pivot):** docs/09 has landed (arbitrary-user public stars and public Lists verified), the $0 free-tier launch ([13](13-free-tier-feasibility.md)–[15](15-free-semantic-search.md)) overlays the multi-tenant design, and resolved questions below are marked ✔ with pointers.
> Companions: [08](08-public-service-ux.md) (UX) · [09](09-public-data-and-limits.md) (public data/limits) · [10](10-multitenant-architecture.md)/[13](13-free-tier-feasibility.md)/[15](15-free-semantic-search.md) (architecture) · [14](14-abuse-protection.md) (abuse); search quality: [07](07-search-contract.md)/[15](15-free-semantic-search.md) (docs 16–18 pending). This file records deltas only; no edits to 00–07 are applied here (their banners are final).

## 0. Conventions

- "Old assumption" quotes are verbatim; `…` elides and `[brackets]` annotate. Inside quotes, `·` replaces the `|` that appears in the source's table rows (so the audit table stays parseable).
- **Indexed user** = a GitHub account whose public stars we index; "per user"/"segment" below means that account or its corpus, never an authenticated app user. **Visitor/searcher** = an anonymous user running queries.
- Actions are prioritized in §10; things to deliberately not touch are in §12.

## 1. 00 — Requirements

| Doc §        | Old assumption (quote)                                                                      | New reality                                                                                                                                                     | Impact                                        | Action                                                                       |
| ------------ | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | ---------------------------------------------------------------------------- |
| 00 §1        | "~3,400 GitHub stars (3,448 as of 2026-09-13), and no way to search them properly"          | The problem is generic: any account's public stars are unsearchable at scale; 3,448 is one segment, not the corpus                                              | Positioning, landing copy, "why" narrative    | Rewrite around arbitrary-user public stars                                   |
| 00 §2        | "**starwatch** — a personal search service over the user's GitHub stars"                    | Public multi-tenant service; no login to search                                                                                                                 | Identity, routes, vocabulary                  | Rewrite §2; define "indexed user" vs "visitor"                               |
| 00 §2 users  | "single user (me). Not multi-tenant."                                                       | Multi-tenant by construction; visitors anonymous                                                                                                                | Data model, auth, quotas, ops                 | Replace with "Visitors: anonymous; Indexed users: any public GitHub account" |
| 00 §2 corpus | "~3,400 starred repos (3,448 live 2026-09-13; growing ~10–20/week): metadata + README text" | Corpus is per segment; aggregate = Σ segments; sizes 0 → >100k stars                                                                                            | Sizing, storage ceilings, UI expectations     | Per-segment corpus row + service aggregate; add caps                         |
| 00 §3 U1–U7  | "`search "effect"`"; U7: "find the repo I starred for X"                                    | Same interactions scoped to a username; U7 = "the repo @user starred"                                                                                           | Query taxonomy + MCP args                     | Add username scope to U1–U7                                                  |
| 00 §4 R8     | "initial full backfill + scheduled incremental sync (nightly cron)"                         | Eager-lazy/manual: first demand triggers indexing; refresh by TTL or explicit request — no per-segment cron                                                     | Freshness semantics, scheduling, staleness UX | Rewrite R8; define trigger/TTL policy (§11)                                  |
| 00 §4 R10    | "detect unstars (soft delete), renames, README changes; re-embed only changed chunks"       | Same mechanic, but unstars are only observed on re-sweep; segments can stay stale far longer                                                                    | Staleness, retention                          | Scope to the user→repo relation; staleness badge                             |
| 00 §4 R14    | "secrets only in Worker secrets (GitHub PAT); no third-party data sharing"                  | One GitHub service token (public read; only a rate-limit budget), plus an admin token (`STARWATCH_API_TOKEN` — not named in any doc yet ⚠️); no per-user tokens | Secret inventory, rotation, abuse             | Rewrite R14; name and scope both secrets                                     |
| 00 §4 R15    | "cost: ≤ $5/mo baseline (Workers Paid) + **< $5/mo marginal**; one-time backfill < $1"      | Marginal cost is per segment (embeddings/vectors) and per query (rerank/AI); unbounded with N                                                                   | Budget, quotas, plan tier                     | Replace with service budget + per-segment/per-query caps                     |
| 00 §4 R11    | "CLI: `sync`, `search`, `similar`, `show`, `stats`"                                         | `sync`/`stats` lose single-index meaning; search/`show` need a username                                                                                         | CLI contract                                  | Rewrite in 05 (§6)                                                           |
| 00 §5        | "Multi-user, sharing, teams" (non-goal)                                                     | Multi-user indexing is the product; sharing is inherent (public)                                                                                                | Contradiction: the scope filter inverts       | Delete; replace with "no private data, no accounts/teams/permissions"        |
| 00 §5        | "Star management beyond sync (bulk unstar, star-list write-back)"                           | Still a non-goal — stronger: read-only, writes nothing to anyone's GitHub                                                                                       | Security posture                              | Keep, strengthen                                                             |
| 00 §6        | "**Effect v4 RC** (`effect@4.0.0-rc.115`)"                                                  | Unchanged, but rc.115 contradicts the working rc.112 pin in [02 §1](02-stack-and-pipeline.md) (pre-existing)                                                    | Version pins                                  | Fix rc.115→rc.112 in passing; otherwise none                                 |
| 00 §8        | "“I know I starred something for X” → found in one query, < 30 s from thought to repo URL"  | Any visitor finds any indexed user's star in one query                                                                                                          | Success metric                                | Rewrite for public visitors                                                  |
| 00 §8        | "Zero-maintenance steady state (cron sync runs silently; failures visible in logs)"         | Contradicts eager-lazy sync + a global rate-limit queue; steady state = bounded staleness + healthy queue                                                       | Ops expectations                              | Rewrite criterion                                                            |
| 00 §8        | "Marginal cost stays single-digit $/mo"                                                     | True only with hard caps; cost scales with segments and queries                                                                                                 | Cost controls                                 | Keep target; add caps + dashboards                                           |

## 2. 01 — Search & Index

| Doc §   | Old assumption (quote)                                                                  | New reality                                                                                                                                                                                                                              | Impact                                                         | Action                                               |
| ------- | --------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- | ---------------------------------------------------- |
| 01 §3.2 | "Estimated corpus: ~19.7k chunks + ~3.3k summary vectors ≈ **23k vectors**"             | Per segment; aggregate = Σ; a repo starred by many users duplicates chunks/vectors unless deduped                                                                                                                                        | Vector count + dims grow linearly; dedupe opportunity          | Update sizing; decide dedupe vs isolation (§11)      |
| 01 §5   | "Vectorize `starwatch` … ~23.5M stored dims"                                            | ✔ Resolved: paid path = **per-user namespace** per indexed user ([10 §3](10-multitenant-architecture.md)); $0 path = repo-level R2 blobs + in-Worker kNN ([15 §2](15-free-semantic-search.md)) — Vectorize is off the free critical path | Free/paid topology split; delete/re-index/eviction hinge on it | Implemented in 10 §1.1/§3, 15 §2                     |
| 01 §5   | "All well within free allowances (D1 5 GB, R2 10 GB…)"                                  | Per segment yes; ~15–60 MB D1 + ~26 MB R2 each → single-D1 ceiling binds in the low hundreds of segments ⚠️                                                                                                                              | Scale ceiling                                                  | Define sharding/eviction thresholds                  |
| 01 §6 C | "D1 is simpler for one shared corpus. Revisit if we ever go multi-corpus/per-user."     | We are multi-corpus; shared D1 with scoping may still win, but the parenthetical is now live                                                                                                                                             | Architecture choice                                            | Re-verify vs namespace/index-per-segment             |
| 01 §8   | "Reranker (300 searches × 50 passages) … ~$0.01"                                        | Per-query cost linear in public traffic; a hot segment makes it unbounded                                                                                                                                                                | Cost/abuse                                                     | Keep flag-controlled; per-IP/global query caps       |
| 01 §8   | "Vectorize queries (few hundred/mo) … $0 (50M dims/mo included)"                        | Public traffic ≫ that; queries become a real line item                                                                                                                                                                                   | Cost                                                           | Budget + repeated-query caching ⚠️                   |
| 01 §8   | "Stable through ~10× usage growth. First cost that grows: Vectorize stored dimensions." | 10× a personal corpus ≠ multi-tenant (10²–10⁴×); stored dims and queries both grow                                                                                                                                                       | Capacity planning                                              | Recompute at target segment/query counts             |
| 01 §3.1 | "One FTS5 table over `full_name`, `description`, `topics`, `readme`"                    | Needs segment scoping; the same repo in N segments can share metadata but duplicates FTS rows                                                                                                                                            | D1 size, query plans, deletes                                  | Add owner key; decide shared-repo FTS vs per-segment |
| 01 §4   | Filter matrix (language/stars/archived/starred/license/topics)                          | Filter semantics unchanged; user scope is an implicit extra predicate; topic post-filter caveat unchanged                                                                                                                                | Contract churn                                                 | Keep filters; add scope in 07                        |
| 01 §7   | RRF + boosts + mode routing                                                             | Unchanged per segment                                                                                                                                                                                                                    | —                                                              | None (§12)                                           |

## 3. 02 — Stack & Pipeline

| Doc §          | Old assumption (quote)                                                                         | New reality                                                                                                                                                    | Impact                                            | Action                                                                          |
| -------------- | ---------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------- |
| 02 §5 auth     | "fine-grained PAT, User permission **"Starring: Read"** … Stored as a Worker secret"           | A PAT can only read _its owner's_ `/user/starred`; arbitrary users need public endpoints. The service token needs **no scopes** (public data)                  | Entire ingestion auth model                       | Rewrite: service token for rate limits only                                     |
| 02 §5 listing  | "`GET /user/starred?per_page=100&sort=created&direction=asc` … → 35 requests for ~3,448 stars" | Becomes `GET /users/{login}/starred` (public; ✔ `star+json` + `sort=created` verified in [09 §1.2](09-public-data-and-limits.md)); private stars never visible | Endpoint, ETag keys, per-user page counts         | Rewrite; key ETags per indexed user                                             |
| 02 §5 ETag     | "Store **ETag per page** in D1; unchanged pages return 304 = **free**"                         | Still true, but keyed `(login, page)`; README ETags are repo-global and can be shared across segments                                                          | Table keys; cross-segment reuse of README fetches | Rewrite key; consider a global repo/README cache                                |
| 02 §5 pipeline | "Cron (nightly) └─► Workflow "star-sync" (durable, checkpointed in D1)"                        | Concurrent per-segment runs initiated by demand, not one cron; a global rate budget serializes them                                                            | Workflow topology, concurrency limits ⚠️, queue   | Per-segment workflow IDs + global scheduler                                     |
| 02 §5 unstar   | "Unstar → soft delete (`is_starred=0`) + `deleteByIds` vectors; keep rows ~90 days"            | Repo starred by A and not B; deleting shared vectors/rows on one unstar corrupts other segments                                                                | Data model + vector IDs                           | Split global `repos` from per-segment `user_stars`; delete segment vectors only |
| 02 §5 budget   | "Budget: 33 list + ~3.3k READMEs ≈ **66% of one 5,000 req/hr window**"                         | That is one segment's backfill; with one token the window is service-global: ≈1.4 backfills/hour max, or ≈70–125 nightly sweeps ⚠️                             | Throughput ceiling, fairness, queue design        | Global rate-limit budget + scheduler (new component)                            |
| 02 §5 secrets  | "Stored as a Worker secret" [GitHub PAT] + 05 §6.2 "starwatch token (`sw_…`)"                  | Collapse into **service GitHub token** (public read) + **admin `STARWATCH_API_TOKEN`** (triggers/ops); no per-visitor token                                    | Secret inventory, rotation, least privilege       | Document both in 02 §5; delete `sw_…` model                                     |
| 02 §5 sizing   | "Sizing (~3,450 repos, avg 6 KB README; scales linearly)"                                      | Still linear per segment; aggregate unbounded                                                                                                                  | Cost/limits                                       | Keep per-segment table; add aggregate row                                       |
| 02 §3.6        | "6 simultaneous outbound connections max → cap GitHub fetch concurrency at 5"                  | Cap is per Worker invocation; concurrent segment runs multiply it; GitHub's 100-concurrency is account-wide                                                    | Scheduler must coordinate globally                | Central limiter/pacing                                                          |
| 02 §7          | "traces + logs to Grafana/Axiom; `persist: false` unless dashboard storage is wanted"          | Public scale makes full-fidelity traces costly/noisy; sync + rate-limit + abuse need dashboards                                                                | Observability cost                                | Sampling + dedicated rate-limit/abuse metrics                                   |
| 02 §8 Q1       | "Backfill trigger: run full sync automatically on first deploy, or manual first run?"          | Replaced by: first search auto-index, TTL refresh, explicit re-index (policy undefined)                                                                        | Trigger semantics                                 | Rewrite question; resolve in §11                                                |
| 02 §4          | "**Alchemy v2** … deploys D1/R2/Vectorize/Queues, PR stages"                                   | Unchanged for the service; only Access wiring changes (06)                                                                                                     | —                                                 | None                                                                            |
| 02 §6          | "Proposed monorepo layout"                                                                     | Unchanged; `core`/`contracts` need segment parameters                                                                                                          | Small type churn                                  | Parameterize; no restructure                                                    |

## 4. 03 — Sync & Limits

| Doc §         | Old assumption (quote)                                                                                          | New reality                                                                                                                                               | Impact                                            | Action                                                                  |
| ------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ----------------------------------------------------------------------- |
| 03 §0         | "`GET /user/starred?per_page=100` … **33 pages** / **35 pages**"                                                | Public equivalent `/users/{login}/starred`; per-segment page counts vary (0 stars → >100k)                                                                | Unbounded work per segment                        | Rewrite headline table parametrically; add caps                         |
| 03 §0         | "Anonymous `/users/coldter/starred` shows 3,446 public; the authenticated token sees **2 private stars**"       | We now build on the 3,446-public model for everyone; private stars are out of scope by design                                                             | Removes an entire caveat; changes expected counts | Rewrite: public-only; never claim parity with profile totals            |
| 03 §1.1       | "Primary limits (per authenticated user, shared by all tokens/apps on that account)"                            | One service account ⇒ one 5,000/h bucket for **all** segments; each backfill ≈70% of it                                                                   | Global contention, queueing, fairness             | Global limiter + scheduler; assume ~1.4 backfills/h                     |
| 03 §1.2       | "Practical envelope for starwatch: concurrency 5, sustained ≤700 req/min, burst ≤12 req/s"                      | Still the envelope, now shared by every concurrent segment run                                                                                            | Central pacing                                    | Put numbers in the scheduler, not the pipeline                          |
| 03 §1.3       | "401 · Token expired/revoked · `needs_auth` terminal state; surface "run `starwatch login`""                    | No visitor login exists; 401 is a service-token outage                                                                                                    | Error taxonomy                                    | Remap to internal ops states + alerting                                 |
| 03 §2(a)      | "Login / auth" [device flow]                                                                                    | Deleted: no per-user GitHub auth                                                                                                                          | Section obsolete                                  | Drop §2(a), or keep as admin bootstrap                                  |
| 03 §2(b)      | "`GET /user` … cached in D1 `profile` … Refresh only on login"                                                  | Profile becomes a per-segment on-demand cache (avatar/link meta)                                                                                          | Table scoping                                     | Key by GitHub user id; refresh policy                                   |
| 03 §2(d)      | "**Private stars**: `/user/starred` returns private repos the token can see … compare totals at login and warn" | Removed; the public listing is the whole corpus                                                                                                           | Simplifies sync; changes counts                   | Delete caveat; `starring=read` no longer needed                         |
| 03 §2(e)      | "query { viewer { lists …"                                                                                      | ✔ Resolved: `user(login).lists` reads public Lists for arbitrary users ([09 §2](09-public-data-and-limits.md)); private lists are owner-only              | Read-only public import stands                    | Spec import as optional enrichment ([08 §4.1](08-public-service-ux.md)) |
| 03 §2(g)      | "Unstar detection requires the **full sweep**"                                                                  | Still true per segment, but full sweeps for all segments are unaffordable                                                                                 | Freshness vs quota                                | Eager-lazy full sweeps on TTL; stale membership accepted                |
| 03 §2(h)      | "`pushed_at > readme_checked_at`" trigger policy                                                                | Still valid per repo; repo-global README work can be shared across segments                                                                               | Dedupe opportunity                                | Consider a global README/vector cache                                   |
| 03 §3         | "**Fine-grained PAT** ✅ recommended … `starring=read`"                                                         | Replaced by one no-scope service token; classic/OAuth/App options were per-owner and vanish                                                               | §3 rewritten                                      | Keep "one secret, least privilege" principle                            |
| 03 §3         | "**Private-stars visibility caveat**" / "**SSO caveat**"                                                        | Both disappear (public data only)                                                                                                                         | Removes diagnostics                               | Delete                                                                  |
| 03 §4.1       | "Onboarding: `starwatch login`"                                                                                 | Obsolete; replaced by admin service-token setup (ops doc)                                                                                                 | CLI docs                                          | Drop from visitor flow                                                  |
| 03 §4.2       | "One Workflow instance per run: id `sw-sync-{run_id}`"                                                          | Per-segment runs; IDs must include the segment; many concurrent instances                                                                                 | Workflow limits ⚠️, collision-free IDs            | `sw-sync-{user_id}-{run_id}` + global cap                               |
| 03 §4.3       | "Schedule: nightly cron (default **03:00 UTC**, adjustable) + `starwatch sync` manual"                          | No nightly for all; manual/eager-lazy + optional hot-segment refresh                                                                                      | Cron design                                       | Rewrite schedule section                                                |
| 03 §4.3       | "`⏸ GitHub rate limit reached — 812/5,000 left; resuming automatically at 11:23 UTC`"                           | With a shared token, one segment's pause blocks/pauses others; message must become queue/fairness aware                                                   | UX + fairness                                     | Redesign as queue position/ETA                                          |
| 03 §4.4       | "`GET /api/sync/status` JSON (CLI polls; shared with the WebUI stream)"                                         | Becomes per-segment status; many visitors may share one stream                                                                                            | Contract                                          | Add `user` param; define shared stream                                  |
| 03 §5 #6      | "Repo goes private … Keep row, flag `visibility_unknown`; never hard-delete on 404"                             | Still possible (public→private), minus token-visibility nuance                                                                                            | Keep, simplify                                    | Edit wording                                                            |
| 03 §5 #10     | "Revoked/expired token … `needs_auth`, halt, banner"                                                            | Service incident, not visitor action                                                                                                                      | Keep as ops error                                 | Remap                                                                   |
| 03 §5 #14–#15 | "Org SSO" / "Org blocks PATs/OAuth apps"                                                                        | Mostly disappear: public resources need no org authorization                                                                                              | Delete rows                                       | Delete                                                                  |
| 03 §5 (new)   | —                                                                                                               | New cases: username not found / renamed / deleted; 0-star accounts; >100k-star accounts; segment never indexed; private-only stars; spam-starred accounts | New edge-case table                               | Add to 03 §5                                                            |
| 03 §6         | "Private stars: index them? … Login method final call … Backfill trigger"                                       | Q2 resolved (no), Q3 resolved (none), Q4 redefined                                                                                                        | Open questions list                               | Rewrite §6                                                              |

## 5. 04 — Groups

| Doc §    | Old assumption (quote)                                                             | New reality                                                                                                                           | Impact                                         | Action                                                                                                                  |
| -------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 04 §1    | "**Local-first**: D1 owns groups; GitHub Lists are optional, never required"       | Under no-login, "local" has no owner: visitors have no identity; groups belong to an **indexed user** (per-user), not to visitors     | Ownership contradiction                        | Decided v1: read-only per-user groups + generated collections; no anonymous writes ([08 §4.3](08-public-service-ux.md)) |
| 04 §1    | "Read-only **one-time import** of the 22 existing lists into local groups"         | ✔ Resolved: public Lists are readable for arbitrary users ([09 §2](09-public-data-and-limits.md)); private lists are not              | Read-only public import only                   | Keep import read-only; no writes ([08 §4.1](08-public-service-ux.md))                                                   |
| 04 §2.1  | "Read lists … `viewer.lists(first/last/after/before)`"                             | ✔ Verified: `User.lists` exists and public lists are readable for any account ([09 §2](09-public-data-and-limits.md))                 | Import is feasible                             | Read-only public import ([08 §4.1](08-public-service-ux.md))                                                            |
| 04 §2.1  | "Observed on `coldter` (2026-09-13): **22 lists** … **279 items total**"           | Owner-specific sample; becomes an illustration only                                                                                   | Copy/statistics                                | Generalize; move sample to an appendix                                                                                  |
| 04 §4.1  | "`slug TEXT NOT NULL UNIQUE`"; `group_members(group_id, repo_id)`                  | Needs an owner/segment column; global slug uniqueness breaks across segments                                                          | DDL + all queries                              | Redesign `0002` with owner key + composite uniqueness                                                                   |
| 04 §4.2  | "always with `is_starred = 1` unless `--include-unstarred`"                        | Per-segment star relation, not a global flag                                                                                          | Rule compiler                                  | Join `user_stars`                                                                                                       |
| 04 §4.4  | "Repo **unstarred** … Local membership **stays** …"                                | Per segment; a repo can remain starred elsewhere, so global retention can't follow one unstar                                         | Lifecycle                                      | Rework per segment                                                                                                      |
| 04 §5.3  | "`repo_id` as a **number metadata index from day 1** (6th of 10 allowed)"          | Couples to index topology; per-namespace metadata indexes may not exist ⚠️; index-per-segment needs per-index setup                   | Topology coupling                              | Re-evaluate after the topology decision                                                                                 |
| 04 §6.1  | "`GET /groups` / `POST /groups` / `PATCH /groups/:slug`"                           | Needs segment scope and auth (`/u/{login}/groups`), plus write rules                                                                  | Contract                                       | Re-scope routes; admin-only writes                                                                                      |
| 04 §7    | ">32 groups vs GitHub cap … mirror flag only on ≤32 selected groups"               | Push mirror requires the user's own token — unavailable under no-login                                                                | v2 mirror dead unless users opt in with tokens | Drop mirror; keep read-only import                                                                                      |
| 04 §8    | "GitHub · ✅ read-only one-time import"                                            | ✔ Public-list import verified for arbitrary users ([09 §2](09-public-data-and-limits.md)); the write mirror is dropped under no-login | Scope table                                    | Read-only scope confirmed                                                                                               |
| 04 §9 Q1 | "Mirror write scope — do Lists mutations work with the existing fine-grained PAT…" | Moot under no-login                                                                                                                   | —                                              | Close question                                                                                                          |

## 6. 05 — CLI

| Doc §     | Old assumption (quote)                                                                                | New reality                                                                                                       | Impact                             | Action                                                  |
| --------- | ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------- |
| 05 §2     | "`login` Authenticate this machine (device flow or token)"; "`logout` Forget the stored token"        | No visitor auth; login only for admin/ops (`STARWATCH_API_TOKEN`)                                                 | Command tree semantics             | Repurpose as admin login or hide; drop from public help |
| 05 §3.1   | "`doctor` — Diagnose config, API, auth, D1, Vectorize, R2, GitHub rate limit"                         | Split: visitor mode (reachability, indexed?, staleness) vs admin mode (bindings, token, queue)                    | UX                                 | `doctor` / `doctor --admin`                             |
| 05 §3.2   | "`sync` … if a run is active, attach instead of starting a second"                                    | "Active" is global now; who may trigger a segment? Add `index <login>` / `sync --user <login>`                    | Command redesign                   | Add segment arg + trigger gating                        |
| 05 §3.3   | Search flags (`--lang`, `--group`, `--topic`, …)                                                      | Add `--user <login>`; required for scoped search unless a default/global search exists                            | Every search/`similar`/`show` call | Add scoping flag; decide the empty default              |
| 05 §3.4   | "`starwatch show BurntSushi/ripgrep`"                                                                 | Same repo may exist in many segments; needs segment context                                                       | Command semantics                  | `show <login>/<repo>` or `--user`                       |
| 05 §3.5   | Groups commands                                                                                       | Blocked on ownership (04)                                                                                         | Command group                      | Defer/gate                                              |
| 05 §4.4   | "Codes: `BAD_FILTER`, `UNAUTHENTICATED`…"; exit 3 "not authenticated / token rejected (401/403)"      | Anonymous use is normal; add `NOT_INDEXED`, `RATE_LIMITED`; 3 = admin token rejected                              | Output contract                    | Edit codes; redefine exit 3                             |
| 05 §6.1   | config `{ apiUrl, token: "sw_…" }`                                                                    | `token` optional (admin only); public API default                                                                 | Config                             | Update example + redaction rules                        |
| 05 §6.2   | "The CLI holds a **starwatch token** (`sw_…`) … non-public endpoints require `Authorization: Bearer`" | Public endpoints require nothing; privileged ones use `STARWATCH_API_TOKEN` (or an Access service token, 06 §8.5) | Auth model                         | Rewrite §6.2                                            |
| 05 §6.3   | "Device-flow UX (default login)"                                                                      | Deleted                                                                                                           | Section                            | Drop; docs/03 cross-ref dies                            |
| 05 §7     | "Unauthenticated: fail fast with `Run: starwatch login`; exit 3"                                      | Anonymous is the default; only admin ops see 401                                                                  | Edge cases                         | Rewrite                                                 |
| 05 §9     | "`Index 3,277 repos · 19,842 chunks · 23,105 vectors`"; "PASS auth"                                   | Mocks become per-segment/service; "auth PASS" only in admin mode                                                  | Examples                           | Refresh mocks                                           |
| 05 §10 Q2 | "Auth model: is device flow worth its moving parts for a single user…"                                | Moot                                                                                                              | —                                  | Close                                                   |

## 7. 06 — WebUI

| Doc §     | Old assumption (quote)                                                                             | New reality                                                                      | Impact                                      | Action                                                     |
| --------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ------------------------------------------- | ---------------------------------------------------------- |
| 06 §1     | "**Cloudflare Access** (GitHub IdP) … — no app-level token"                                        | Cannot gate a public service; Access only on admin/trigger paths + previews      | Architecture                                | Anonymous public read; Access/service token for admin      |
| 06 §1     | "Groups · docs/04 model: local-first manual groups"                                                | Ownership unresolved (04)                                                        | UI scope                                    | Gate after the 04 decision                                 |
| 06 §2     | "there is no server rendering or SEO need (single user, behind Access)"                            | Public pages need discoverability and link previews; SPA-only may miss SEO       | Contradicts §3 "bookmarkable and shareable" | Decide prerender/SSR for public routes (§11)               |
| 06 §2     | "`/` Search"; "`/repo/:owner/:name` Repo detail"                                                   | Missing username context; reserved-route collisions (`/groups`, `/sync`, …)      | IA + URL design                             | Add `/:login`, `/:login/repo/...`; reserve words           |
| 06 §3     | "empty query — recent stars, top groups, corpus stats (`3,277 repos · 23k chunks · last sync 2h`)" | Per-segment stats; tiny/empty segments common                                    | UI copy + empty states                      | Per-segment stats + service landing page                   |
| 06 §4     | "starwatch never writes to GitHub in v1"                                                           | Still true, stronger                                                             | —                                           | Keep                                                       |
| 06 §5     | "Create / rename / delete … reorder" (groups manager)                                              | Anonymous visitors can't own groups                                              | Ownership contradiction                     | Read-only groups for visitors; admin editing               |
| 06 §6     | "**Sync now** triggers `POST /api/sync` (idempotent: 409 …)"                                       | Any visitor triggering indexing of any username = abuse + quota drain            | Abuse/cost                                  | Auto-index on first view + TTL; per-IP limits; admin force |
| 06 §6/§11 | "one worker instance streams to one browser; no fan-out"                                           | Many visitors may watch the same segment's sync                                  | SSE design                                  | Shared stream/broadcast, or reconnect + status             |
| 06 §7     | "Access · Signed-in identity"; "Danger zone: full re-embed, remove all non-starred repos"          | Admin-only; public settings shrink to preferences                                | IA                                          | Split admin console vs visitor prefs                       |
| 06 §8.5   | "one allow policy for the owner's GitHub account/email … protect a single-user app"                | Access for an admin prefix only                                                  | Deployment                                  | Rewire Alchemy `access` to `/api/admin/*` (+ previews)     |
| 06 §11 Q2 | "Trivial at 3.3k rows" (facet counts)                                                              | Aggregate corpora larger; per-segment still small, but hot segments + query load | Latency budget                              | Verify under load; index scoped query paths                |
| 06 §11 Q4 | "SSE back-pressure — one worker instance streams to one browser; no fan-out"                       | Fan-out question above                                                           | —                                           | Rewrite                                                    |

## 8. 07 — Search Contract

| Doc §    | Old assumption (quote)                                                                        | New reality                                                                                 | Impact              | Action                                                      |
| -------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ------------------- | ----------------------------------------------------------- |
| 07 §1    | "Corpus: **3,448 stars** (live 2026-09-13; earlier docs said 3,277 — a truncated pagination)" | Per segment; 3,448 is the first segment; every baseline becomes "for the reference segment" | Contract intro      | Restate per segment                                         |
| 07 §1    | Baseline facts ("README present 108/110", "Topics empty 25.2%", …)                            | Useful priors, but cross-segment variance is unknown; 0-star segments exist                 | Expectations        | Mark as sample priors, not invariants                       |
| 07 §2    | "Seven classes" + "Routing in `auto` mode"                                                    | Classes survive; examples are one segment's                                                 | Eval wording        | Add per-segment examples; no logic change                   |
| 07 §3.3  | "**Size:** 60–100 queries, stored in-repo as `eval/golden.yaml`"                              | One golden set can't cover arbitrary corpora; need per-segment sets or synthetic corpora    | Eval harness        | Multi-corpus eval plan; frozen fixtures for CI              |
| 07 §3.5  | "Index drift > 1% since baseline makes regression comparison invalid"                         | Continuous indexing makes drift the norm                                                    | Regression policy   | Pin a snapshot per eval; local fixtures already required ⚠️ |
| 07 §4.1  | "groups · user-defined slugs, repeatable"                                                     | Scoped per segment; ownership unresolved                                                    | Filter contract     | Add owner scope (blocked on 04)                             |
| 07 §4.4  | "Groups are user-defined labels … `groups(id, slug, name)` + `repo_groups`"                   | Same as 04: per segment; the contract must carry the owner                                  | Storage + API       | Update with 04                                              |
| 07 §5.3  | "Star prior"; "Starred recently" boosts                                                       | Per-segment signals; the same repo has different `starred_at` per segment                   | Ranking inputs      | Scope boosts per segment; RRF unchanged                     |
| 07 §5.4  | "≈ $0.01–0.03/mo at our usage" (rerank)                                                       | Linear per query at public scale                                                            | Cost                | Keep `auto`; add query caps                                 |
| 07 §8    | "Cross-user/global GitHub search or recommendations; feed/trending" (out of scope)            | Multi-user indexing is in scope; cross-segment search must be explicitly decided            | Scope contradiction | Reframe: per-segment search v1; cross-segment = open q      |
| 07 §8    | "Personalization from clickstream (none collected)"                                           | Still none; anonymous query stats may be needed for quotas/abuse                            | Privacy             | Keep; add a log-retention policy                            |
| 07 §9 Q1 | "**Groups in v1?** Not in 00 §4; this contract defines them anyway"                           | Still open + ownership                                                                      | —                   | Resolve with 04                                             |
| 07 §9 Q6 | "Corpus count drift — resolved … canonical count is 3,448"                                    | Resolved for one segment; now N corpora                                                     | —                   | Restate as reference-segment count                          |

## 9. Contradictions now present between docs

1. **Auth triangle void.** [03 §3](03-sync-and-limits.md) ("fine-grained PAT ✅ recommended", `starring=read`) + [05 §6.3](05-cli.md) (device flow default) + [06 §8.5](06-webui.md) (Access for a single user) all assume the owner authenticates GitHub into the Worker. The pivot has one service token and anonymous visitors.
2. **Non-goal inversion.** [00 §5](00-requirements.md) "Multi-user, sharing, teams" vs the entire public-service premise and every group-ownership section.
3. **Cron vs eager-lazy.** [00 R8](00-requirements.md) + [03 §4.3](03-sync-and-limits.md) "nightly cron" vs the pivot's manual/eager-lazy sync.
4. **Private stars.** [03 §2(d)/§3/§6 Q2](03-sync-and-limits.md) + [02 §5](02-stack-and-pipeline.md) ("+ repo access if private stars should be indexed") vs public-only; the old 3,448 count included 2 private stars.
5. **Lists visibility.** ✔ Resolved: [09 §2](09-public-data-and-limits.md) verified that `User.lists` exposes any user's public Lists; private lists are owner-only.
6. **Group ownership.** [04 §1](04-groups.md) "local-first, D1 owns groups" + [06 §5](06-webui.md) CRUD vs no-login visitors with no identity.
7. **SEO.** [06 §2](06-webui.md) "no server rendering or SEO need (single user, behind Access)" vs [06 §3](06-webui.md) "Every search is bookmarkable and shareable" and public discoverability.
8. **Access vs public.** [06 §8.5](06-webui.md) Access "gates … static assets and SSE" vs public pages; [05 §6.2](05-cli.md) "non-public endpoints require `Authorization: Bearer`" vs anonymous search.
9. **SSE fan-out.** [06 §11 Q4](06-webui.md) "one worker instance streams to one browser; no fan-out" vs many visitors watching one segment's sync.
10. **Cross-user search scope.** [07 §8](07-search-contract.md) "Cross-user/global GitHub search … out of scope" vs multi-user indexing; no doc defines whether search crosses segments.
11. **Eval pinning.** [07 §3.3–§3.5](07-search-contract.md) single-corpus golden set and "drift > 1%" invalidation vs continuous indexing of arbitrary segments.
12. **Quota model.** [03 §1.1](03-sync-and-limits.md) "per authenticated user, shared by all tokens/apps on that account" — one account now serves all segments; the 66–70%-of-window backfill math is per corpus but the window is global.
13. **Cost growth.** [01 §8](01-search-and-index.md) "Stable through ~10× usage growth" vs public scale; [01 §5](01-search-and-index.md) free-allowance claims vs aggregate storage.
14. **Index topology.** ✔ Resolved for the $0 launch: repo-level R2 blobs + in-Worker kNN ([15 §2](15-free-semantic-search.md)); paid path = per-user Vectorize namespaces ([10 §3](10-multitenant-architecture.md)).
15. **CLI mocks.** [05 §9](05-cli.md) status/doctor outputs show a single index and "PASS auth" vs per-segment/service stats and anonymous use.
16. **(Pre-existing, not pivot)** [00 §6](00-requirements.md) pins `effect@4.0.0-rc.115` while [02 §1](02-stack-and-pipeline.md)/README pin rc.112 for Alchemy compatibility.

## 10. Minimal edit plan (priority order; audit trail)

**Status 2026-09-13:** the decisions below are captured in docs 08–15; docs 00–07 carry final free-tier banners and are frozen. This list remains the audit trail, not open work.

**P0 — blockers; decide before editing prose**

- [00](00-requirements.md) §2: rewrite product statement, users row, corpus row; §5: replace the multi-user non-goal; §8: rewrite success criteria.
- [03](03-sync-and-limits.md) §3 + §4.1 + §6: remove the per-user PAT/device-flow/login model; move admin setup to an ops doc.
- [02](02-stack-and-pipeline.md) §5: rewrite auth (service token), listing endpoint (`/users/{login}/starred`), ETag keys, unstar/vector deletion, workflow topology.
- Data model: split global `repos` from per-segment `user_stars`; decide the Vectorize topology (namespaces vs `user_id` vs index-per-segment) in a new design doc — do not bury it in 00–07 edits.
- ✔ Lists/import edits unblocked and applied ([09 §2](09-public-data-and-limits.md), [08 §4](08-public-service-ux.md)).

**P1 — structural edits once P0 decisions land**

- [03](03-sync-and-limits.md) §1.1–§1.3 + §4.3: global rate-limit budget, queue/fairness, service-level error taxonomy, edge-case table rewrite.
- [01](01-search-and-index.md) §5 + §8: topology, per-segment/aggregate sizing, query/rerank cost caps.
- [04](04-groups.md): ownership model; redesign §4.1 DDL with owner key; drop the v2 mirror; re-scope §6.1 routes.
- [06](06-webui.md) §1/§2/§8.5: split public vs admin auth; username routes; SEO/prerender decision; sync-trigger policy.
- [05](05-cli.md) §3.1/§3.2/§6: command semantics (`index`/`sync --user`, search `--user`), optional token, exit-code 3 redefinition.

**P2 — consistency pass**

- [00](00-requirements.md): R8/R10/R11/R14/R15 wording.
- [07](07-search-contract.md): segment scoping in filters/boosts, multi-corpus eval, latency-under-load note, reframe the cross-user non-goal.
- [03](03-sync-and-limits.md): §5 edge cases (add new, delete SSO/org); §4.4 per-segment status contract.
- [06](06-webui.md): §6/§7 sync + settings split; group editor gating; mock stats.
- [01](01-search-and-index.md): §6 Option C sentence; §10 open decisions.
- Root `README.md` and the docs index: pivot framing + links to the new docs (add 08–11 as they land).

## 11. New open questions created by the pivot

1. **Search scope — ✔ resolved (v1):** per-indexed-user only; cross-user global search is v2 ([08 §3.3](08-public-service-ux.md)).
2. **Vectorize topology — ✔ resolved for $0:** repo-level R2 blobs + in-Worker kNN ([15 §2](15-free-semantic-search.md)); paid path = per-user namespaces ([10 §3](10-multitenant-architecture.md)).
3. **Dedupe — ✔ resolved:** shared corpus + content-addressed embedding cache; users reference shared repo artifacts ([10 §2](10-multitenant-architecture.md)).
4. **Identity — ✔ resolved:** numeric GitHub user id as the canonical key; login for URLs/display; rename handling in [09 §1.1](09-public-data-and-limits.md).
5. **Caps — ✔ resolved:** `MAX_STARS = 10,000`; newest-1,500 semantic window; 64 KB/repo + 20 MB/user FTS; 50 full/warm indexed users ([14 §3.6](14-abuse-protection.md)).
6. **Rate-limit budget — ✔ resolved:** one 5,000/h token, no pooling; governor reserves 300/h ([09 §4.2](09-public-data-and-limits.md), [14 §3.4](14-abuse-protection.md)).
7. **Trigger policy — ✔ resolved:** intent-gated eager-lazy auto-start ([08 §2.2](08-public-service-ux.md)); admission/quotas in [14 §3](14-abuse-protection.md).
8. **Abuse/cost — ✔ resolved:** [14](14-abuse-protection.md) owns budgets, quotas and degradation.
9. **Freshness promise — ✔ resolved:** 15 min re-list / 24 h full refresh / capped accounts 7 d; on-visit stale-while-revalidate ([08 §2.3](08-public-service-ux.md), [10 §6](10-multitenant-architecture.md)).
10. **Groups ownership — still open:** v1 is read-only (no anonymous writes); v2 owner-verified OAuth ([08 §4.3](08-public-service-ux.md)).
11. **Lists import — ✔ resolved:** public Lists are readable for arbitrary accounts; private lists are not ([09 §2](09-public-data-and-limits.md)); import is optional enrichment.
12. **Eval — still open:** multi-corpus golden sets and drift handling; docs 16–18 pending.
13. **SEO/privacy — partially resolved:** shareable `/u/*` with robots/sitemap/noindex policy and a purge flow ([08 §5.2](08-public-service-ux.md), [12 §5](12-hardening.md)); retention windows remain open.

## 12. What does NOT change (churn control)

- **Search modes and taxonomy** U1–U7 / Q1–Q9; hybrid default; identifier routing; RRF k=60; boosts; `--rerank auto`; snippets/highlighting; query parsing and FTS5 safety rules.
- **Stack:** Effect v4 RC pin (rc.112) + Alchemy `2.0.0-beta.77` pin; Workers runtime; D1 FTS5 (porter + trigram); `bge-m3` embeddings; summary vectors; chunker v1.
- **Roles:** D1 = metadata + FTS5 + chunks; Vectorize = vectors; Workers AI = embeddings/rerank; R2 = raw README archive; avatars hotlinked from GitHub CDN.
- **Sync mechanics:** idempotency, checkpoints, per-chunk hashing, ETag/304 optimization, stable `repo.id`, soft-delete retention (duration may change), Workflow step/subrequest constraints.
- **Surfaces:** CLI + WebUI first, MCP v2; `--json`/`--plain` discipline; exit codes 0/1/2/130 (3 is redefined, not removed).
- **Filters vocabulary and naming parity** across CLI/HTTP/WebUI/MCP.
- **Eval methodology:** R@10 / nDCG@10 / MRR / Success@3 / filter precision; `starwatch eval` shape; latency budget method (targets re-validated under multi-tenant load).
- **Alchemy deployment** approach and Cloudflare observability tooling (add sampling; don't switch vendors).

## Sources / verification needed ⚠️

No new external claims are load-bearing here; quotes are from docs 00–07 (2026-09-13 revisions). Verified since (2026-09-13): `GET /users/{login}/starred` handling of `application/vnd.github.star+json` and `sort=created`, and `User.lists` visibility ([09 §1–§2](09-public-data-and-limits.md)); Vectorize namespace limits and free-plan caps ([10 §3](10-multitenant-architecture.md), [13](13-free-tier-feasibility.md)); free semantic path and CPU benchmarks ([15](15-free-semantic-search.md)). Still to verify: D1 max database size and row-read pricing; Workflows concurrent-instance limits; Workers AI paid-tier embedding/rerank pricing at volume.
